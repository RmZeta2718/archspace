# Install OLMo-core from the source branch that contains the Muon fixes used by
# this reproduction. Hardware-specific FlashAttention must be installed
# separately; see README.md.
ai2-olmo-core[all] @ git+https://github.com/JT-Ushio/OLMo-core-muon-fix.git@ready_for_archspace_base
